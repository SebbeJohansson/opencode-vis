# Copied by `nuxt dev` (never committed). The bin/openui.mjs launcher also
# accepts the legacy names OPENCODE_URL / EXPERIMENTAL_CLAUDE / VIS_PORT.
NUXT_OPENCODE_URL=http://localhost:4096
NUXT_CLAUDE_ENABLED=false
# NUXT_CLAUDE_BIN=/usr/local/bin/claude
